/*
  The marketing group's only job is to stay out of the way: the landing page
  paints its own grounds edge to edge, so there is no shell, no gutter and no
  max-width here. Fonts come from the root layout.
*/
export default function MarketingLayout({ children }: LayoutProps<"/">) {
  return children;
}
